@model Jogador

@{
    ViewData["Title"] = "Jogador";
    
}


<form method="post" action="@Url.Action("Cadastrar")" >

<label for="">Nome</label>
<input placeholder="Digite o seu nome..." type="text" name="Nome" required>

<label for="">Email</label>
<input type="text" placeholder="Digite seu email..." name="Email" required>

<label for="">Senha</label>
<input type="text" placeholder="Digite sua senha" name="Senha" required>
<label for="">IdEquipe</label>
<input type="text" placeholder="Digite id equiupe" name="IdEquipe" required>

<button class="btn btn-warning" type="submit">Cadastrar</button>
</form>




<table class="table table-striped table-responsive"> 
    <thead>
        <th>Id</th>
        <th>Nome</th>
        <th>Email</th>
        <th>Equipe</th>
        <th>IdEquipe</th>
        <th>Foto</th>
        <th></th>
        
    </thead>

    <tbody>
           @foreach (Jogador jogador in ViewBag.Jogador)
           {
        <tr>
                <td>@jogador.IdJogador</td>
                <td>@jogador.Nome</td>
                <td>@jogador.Email</td>
                <td>@jogador.IdEquipe</td>
                @* Realizar a criação do menu posteriormente *@
                <td><a asp-area="" asp-controller="Jogador" asp-action="Excluir" asp-route-id="@jogador.IdJogador" style="text-decoration: none;">🗑️</a></td>
                <td><a asp-area="" asp-controller="Jogador" asp-action="Editar" asp-route-id="@jogador.IdJogador" style="text-decoration: none;">✏️</a></td>
        </tr>
           }
            
    </tbody>
</table>